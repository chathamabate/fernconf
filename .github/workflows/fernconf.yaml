name: Publish

on:
  push:
    tags:
      - "v[0-9]+\\.[0-9]+\\.[0-9]+"

jobs:
  upload:
    name: Upload
    runs-on: ubuntu-latest
    steps:
      - uses: ./.github/workflows/test_and_package.yaml
        with:
          upload: true

  deploy:
    name: Publish
    needs: upload
    runs-on: ubuntu-latest
    permissions:
      id-token: write

    steps:
    - name:
    - name: Download Dist
      uses: actions/download-artifact@v4
      with:
        name: dist
        path: dist/

    - name: Publish package to PyPI
      uses: pypa/gh-action-pypi-publish@release/v1
